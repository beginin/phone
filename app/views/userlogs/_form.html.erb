<%= form_for(@userlog) do |f| %>
  <% if @userlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userlog.errors.count, "error") %> prohibited this userlog from being saved:</h2>

      <ul>
      <% @userlog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :cfu_id %><br />
    <%= f.collection_select(:cfu_id, Cfu.all, :id, :namecfu) %>
  </div>
  <div class="field">
    <%= f.label :sumlimit_id %><br />
    <%= f.collection_select(:sumlimit_id, Sumlimit.all, :id, :namelimit) %>
  </div>
  <div class="field">
    <%= f.label :schedule_id %><br />
    <%= f.collection_select(:schedule_id, Schedule.all, :id, :timein ) %>
  </div>
  <div class="field">
    <%= f.label :userperiod_id %><br />
    <%= f.number_field :userperiod_id %>
  </div>
  <div class="field">
    <%= f.label :sim_id %><br />
    <%= f.number_field :sim_id %>
  </div>
  <div class="field">
    <%= f.label :operator_id %><br />
    <%= f.collection_select(:operator_id, Operator.all, :id, :opername) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
