<h1>Listing Loads</h1>

<table>
  <tr>
    <th>Opername</th>
    <th></th>

  </tr>

<% @loads.each do |f| %>
  <tr>
  	<td><%= f.admin %></td>
    <td><%= f.created_at %></td>
    <td><%= link_to 'Show', :controller => "report", :action => "show", :id => f.id  %></td>
  </tr>
<% end %>
</table>

<br />